// components/pagination.tsx

import React from 'react'

const Pagination: React.FC<PaginationProps> = () => {
  // Jika komponen tidak menggunakan properti, Anda dapat menghapus destrukturisasi properti dari parameter fungsi
  return <div>{/* Konten komponen Anda */}</div>
}

// Tambahkan nama tampilan (display name) untuk komponen
Pagination.displayName = 'Pagination'

export default Pagination
